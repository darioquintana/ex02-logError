export class Logger { 
    log(message){ 
        //Logic to append the new message to a text file.
        //...
        //Just logging the message to see something in the console.
        console.log(message)
    }
}